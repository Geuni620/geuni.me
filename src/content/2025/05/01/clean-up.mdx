---
date: "2025-05-01"
title: "clean up function"
category: ["개발"]
summary: "리액트 19에서 추가된 기능"
---

import { SandpackWrapper } from "@/components/sandpack";
import { REACT_TEMPLATE_VITE_18, REACT_TEMPLATE_VITE_19 } from "./file";

## React 18 (이전 버전)

<SandpackWrapper
  files={REACT_TEMPLATE_VITE_18.files}
  customSetup={REACT_TEMPLATE_VITE_18.customSetup}
  options={REACT_TEMPLATE_VITE_18.options}
/>

## React 19 (clean up 함수)

<SandpackWrapper
template="react-ts"
  files={{
    "/App.tsx": `import { useState, useEffect, cleanup } from "react";

export default function App() {
  const [count, setCount] = useState(0);
  
  useEffect(() => {
    console.log("Effect 실행됨!");
    
    // React 19의 새로운 cleanup 함수 사용법
    return(() => {
      console.log("새로운 Cleanup API 사용!");
    });
  }, [count]);
  
  return (
    <div>
      <h1>React 19 Cleanup</h1>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>증가</button>
    </div>
  );
}`,
  }}
  customSetup={{
    dependencies: {
      "react": "^19.0.0",
      "react-dom": "^19.0.0"
    }
  }}
  options={{
    showConsole: true,
    activeFile: "/App.tsx"
  }}
/>
